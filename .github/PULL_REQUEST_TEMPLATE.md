## What changed

<!-- One paragraph: the problem, the change, and why this shape. -->

## Verification

<!-- The evidence: tests run (and their count), typecheck/lint, and for behavior-facing changes a live run through the built CLI with its run id. -->

- [ ] Tests pass (`pnpm test`)
- [ ] Typecheck and lint pass (`pnpm typecheck`, `pnpm lint`)
- [ ] Behavior verified on the built binary where the change is user-facing
- [ ] Method docs and `fusion skill` text updated if a method changed (ADR-0001)
